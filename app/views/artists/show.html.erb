Scores for <%=@artist.name%>:
<ul>
  <% @user_taken.each do |user_id, count| %>
  	<% user = User.find(user_id) %>
    <li><%= user.username %> - Score: <%= user.artist_score(@artist).round %> - # Taken: - <%= count %></li>
  <% end%>
</ul>